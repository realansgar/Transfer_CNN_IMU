name: transfer_cnn_imu
channels:
  - pytorch
  - defaults
dependencies:
  - tensorboard
  - pytorch
  - tqdm
  - autopep8
  - python=3.8
  - cudatoolkit=10.1
prefix: /home/oskar/anaconda3/envs/transfer_cnn_imu

